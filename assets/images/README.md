# Image Notes

The deployed site uses public Wikimedia Commons image URLs from `src/content.js` so the GitHub repository can stay lightweight and easy to deploy on Vercel.

Local generated PNG drafts may exist in this folder during design work, but they are not required for the deployed site.
